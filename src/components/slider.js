import { Carousel } from "react-responsive-carousel";
import React from "react";



const Slider = ({props}) => {

    const { images } = props
    
    console.log('este es el slider', images)


    return (
        <>
        </>
        
    // <Carousel>
    //       {images.map((img) => ( 
              
    //           <img src={img} />

              
    //       ))}

    
    // </Carousel>
  );
};

export default Slider;
